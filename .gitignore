__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
dist/
build/

# workspace temporário usado pelo bridge headless (scripts + resultado JSON)
.capella-workspace/

# marcador de projeto Eclipse gerado em runtime por _ensure_models_project()
tests/fixtures/.project

# lock file por modelo, criado em runtime por bridge.model_lock()
*.aird.lock
